# Changelog - Week 1, 16-07-2026, V. 0.4.0

**Release Date**: Week 1, 16-07-2026 at 21:00
**Version**: 0.4.0 (previously 0.3.1)
**Object**: feat(viewer): 잔여 블록 렌더러 — 손그림 계층 2/2 (M3 완료)
**Code review**: `docs/3-code-review/CR_w1_v0.4.0.md` (Codex loop, 4 rounds -> APPROVED)

## Changes

M3(시각화 심화) 2차 사이클 — fallback으로 남아 있던 블록 5종(diagram·
annotated-code·data-model·api-endpoint·question-form)이 실제 렌더러로
전환됐다. 전체 블록 셋이 렌더링되어 M3가 완료됐다.

- **diagram (mermaid 11.16.0)**: handDrawn 룩 + `--wf-*` 토큰 실값 매핑,
  block.id 결정적 `handDrawnSeed`, `html.dark` 관찰 재렌더(라이트/다크
  자동 반전). lazy 청크로 격리.
- **mermaid 보안 어댑터**: 렌더 직렬화 큐·시도별 고유 DOM id·테마 세대
  가드(StrictMode 이중 이펙트 안전), 작성자 frontmatter/`%%{init}%%`
  strip 후 신뢰 frontmatter 주입(룩은 렌더러 소유), 외부 리소스 보수적
  사전 거부(`@{…}` 메타데이터 전체 + `url(`/URL 스킴/`//` — YAML 인용 키·
  이스케이프·anchor/alias 난독화 원천 차단), 출력 SVG DOMPurify 후처리.
- **annotated-code**: shiki 하이라이트 + 라인 마커·`startLine` 기반 라인
  번호·마진 주석(반응형, 라인 앵커 링크·react-markdown).
- **shiki 공용화**: `lib/highlighter.ts` 싱글턴 — 동적 lang 로딩·미지
  lang plaintext 폴백(동기 예외 처리). DiffBlock은 동작 불변 위임.
- **data-model / api-endpoint**: 엔티티 카드·관계 목록, 메서드 배지·
  요청/응답 테이블. `FieldTable` 공유, `inferred`는 "추론됨" 배지로
  표시 (Grounding Rule 시각 장치).
- **question-form**: 읽기 전용 질문 목록(실제 폼 컨트롤 없음),
  `allowFreeText` 배지, 응답 수집은 M6 안내.
- **구조 정리**: lazy 경계(에러 바운더리+Suspense) 공용화, FallbackBlock은
  미지 타입 방어로 축소, `--wf-*` 토큰을 `tokens.css`로 분리(diagram 단독
  문서에서도 토큰 보장).
- **픽스처·문서**: sample-plan(diagram·question-form)·sample-recap
  (data-model·api-endpoint·annotated-code, 실제 소스 라인 grounding) 보강,
  ARCHI.md §2·§3·§4·§8, GOAL.md M3 완료 갱신.
- **테스트**: 56건 추가 (총 162건 — viewer 115 + schema 47). 외부 리소스
  우회(인용 키·이스케이프·anchor/alias) 회귀 테스트 포함, kitchen-sink
  전 블록 라이트/다크 Playwright 수동 검증.
