# Changelog - Week 1, 16-07-2026, V. 0.3.0

**Release Date**: Week 1, 16-07-2026 at 15:58
**Version**: 0.3.0 (previously 0.2.0)
**Object**: feat(viewer): 뷰어 MVP — 읽기 전용 렌더링 (M2)
**Code review**: `docs/3-code-review/CR_w1_v0.3.0.md` (Codex loop, 3 rounds -> APPROVED)

## Changes

- **`apps/viewer` 신설**: Vite 8 + React 19 SPA. DESIGN.md 토큰을 Tailwind 4
  `@theme` semantic CSS 변수로 매핑하고 다크 변형을 자체 정의(라이트/다크
  완비). DM Sans + Pretendard Variable 자체 호스팅(한글 폴백).
- **블록 렌더러 6종 + fallback**: prose·callout(react-markdown),
  file-tree(트리 변환, status·inferred·note 표시), tabs·columns(재귀),
  diff(shiki 듀얼 테마 + 헌크 인식 행 배경). 미구현 6종은 M3 안내
  fallback으로 렌더링해 문서가 깨지지 않음.
- **문서 목록·문서 뷰**: 문서 id 해시 기반 그라디언트 카드 식별색(브랜드
  팔레트 조합 8종), kind 배지·메타 표시, 반응형 그리드.
- **문서 로딩 이중 경로**: 내장 픽스처 3종(safeParse 검증) + 파일
  열기/드래그앤드롭(검증 실패 시 인라인 에러).
- **안전 라우팅**: 임의 문서 id(%, /, 유니코드)를 base64url 불투명 라우트
  키로 처리 — react-router 인코딩 왕복 충돌 제거.
- **워크스페이스**: 루트 vitest projects에 `apps/*` 추가, schema에 픽스처
  서브패스 export(additive), 뷰어는 schema를 소스 alias로 소비.
- **테스트**: 뷰어 29건 추가(렌더러 매핑, 파일 열기 통합, 라우트 키, diff
  분류, 테마) — 총 76건. 라이트/다크 렌더링은 Playwright로 수동 검증.
