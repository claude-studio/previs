# Changelog - Week 1, 16-07-2026, V. 0.2.0

**Release Date**: Week 1, 16-07-2026 at 14:28
**Version**: 0.2.0 (previously 0.1.0)
**Object**: feat(schema): 블록 스키마·모노레포 스캐폴딩 (M1)
**Code review**: `docs/3-code-review/CR_w1_v0.2.0.md` (Codex loop, 2 rounds -> APPROVED)

## Changes

- **모노레포 스캐폴딩**: pnpm workspace(`packages/*`, `apps/*`) 구성.
  TypeScript 6.0.3·ESLint 10(flat config)·Prettier·Vitest 4 툴체인 확정,
  루트 검증 명령 `pnpm lint / typecheck / test / build` 도입.
- **`@previs/schema` 패키지**: 12종 블록(prose, callout, file-tree, tabs,
  columns, diff, annotated-code, data-model, api-endpoint, wireframe, diagram,
  question-form)의 zod 4 discriminated union 스키마와 문서 envelope
  (`schemaVersion`·`kind`·`source`) 정의. `parsePrevisDocument` /
  `safeParsePrevisDocument` 공개 API 제공.
- **문서 레벨 검증**: 블록 id 전역 유일성(중첩 포함), 컨테이너 최대 중첩
  깊이 4 제한, annotated-code 발췌 행 범위 검증.
- **픽스처**: M2 뷰어용 `sample-plan.json` / `sample-recap.json`(기본 블록
  6종)과 스키마 커버리지용 `kitchen-sink.json`(12종 전체). Grounding Rule에
  따라 발췌는 실제 소스 라인만 인용.
- **테스트**: 콜로케이션 테스트 15개 파일 47건 (블록별 valid/invalid, 문서
  레벨 경계, 픽스처 round-trip, dist ESM smoke).
- **문서 확정 전환**: ARCHI.md §3·§4·§6·§14와 TESTING.md를 구현 상태로 갱신.
